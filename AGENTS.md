# Agent configuration

```yaml
prompt_logging:
  enabled: false
```

## Local engineering rules

- Stage local plugin builds as a complete directory and restart the Omarchy shell after replacing QML; file-by-file hot reload can retain stale compiled components.
